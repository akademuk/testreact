import React from 'react';
import styles from './HelpContent.module.scss';

const HelpContent = () => {
  return (
    <div className={styles.helpContent}>
      <h2>Вам потрібна допомога?</h2>
      {/* Добавьте здесь содержимое для помощи */}
    </div>
  );
};

export default HelpContent;
